# 单例模式

在一个项目中，某个类的实例有且只有一个，通过这个唯一的实例提供全局访问->不适用全局变量，因为类封装可以控制访问权限

* 任务队列：存储函数指针or可调用对象

### 独生子女

保证单例类的对象有且只有一个的方式。

1. 拷贝构造函数及其操作符重载：`delete`或者 置为`private`并设置为`defalut`
2. 默认构造函数：置为`private`
3. 通过设置`private`静态成员变量、`public`静态成员函数的方式创建实例化对象，注意静态成员变量需要类外初始化

